/*
  1，获取左、右子树根节点下标函数 done
  2，维护最大推性质函数
  3，建立最大堆
  4，堆排序
*/

var left = function(index) {
    return 2 * index
}

var right = function(index) {
    return 2 * index + 1
}

var maxHeapify(array, index) {
    
}
